# AGENTS.md - 硬约束模板

## 验证铁律（Verification Iron Law）

**核心原则**：证据先于断言，"代码存在" ≠ "功能工作"

### 触发条件

准备说"完成"、"搞定"、"done"时，**必须先验证**：

### 验证清单

1. **功能验证**：实际运行代码，检查输出是否符合预期
2. **边界验证**：测试异常情况、边界条件
3. **依赖验证**：确认相关功能未受影响
4. **文档验证**：检查文档是否与代码一致

### 禁止行为

- ❌ 只读代码就说"完成了"
- ❌ 改了配置就说"生效了"（未重启验证）
- ❌ 写了测试就说"通过了"（未实际运行）
- ❌ 改了文件路径就说"修复了"（未测试新路径）

### 验证步骤

```
1. 运行测试命令
2. 检查输出结果
3. 确认无副作用
4. 记录验证结果
5. 才能报告"完成"
```

**来源**：Superpowers `verification-before-completion` skill

---

## 系统化调试（Systematic Debugging）

**硬约束**：禁止在没有根因分析的情况下修复问题

### 四阶段流程

**阶段 1：根因调查**
- 读取错误信息，理解错误类型
- 重现问题，确认复现步骤
- 检查最近的代码变更
- 追踪数据流，定位问题点

**阶段 2：模式分析**
- 查找类似问题的成功案例
- 对比正常情况和异常情况
- 识别关键差异点

**阶段 3：假设验证**
- 一次只测试一个假设
- 设计验证实验
- 记录假设结果

**阶段 4：修复验证**
- 在根因层面修复，不是症状层面
- 验证修复不引入新问题
- 回归测试相关功能

### 调试模板

```markdown
## 问题：[描述]

### 根因分析
- 错误信息：[具体错误]
- 复现步骤：[步骤]
- 根因定位：[根本原因]

### 修复方案
- 修复方法：[具体修复]
- 影响范围：[受影响的功能]

### 验证结果
- 测试命令：[命令]
- 测试结果：[结果]
- 回归测试：[结果]
```

**来源**：Superpowers `systematic-debugging` skill

---

## 强制规划流程（Superpowers 整合）

**核心原则**：禁止跳过规划直接编码

#### 触发条件

- 用户说"帮我构建"、"实现一个功能"、"开发一个模块"
- 任务涉及多个文件或步骤
- 任务预估时间 > 5 分钟

#### 规划流程

**阶段 1：头脑风暴（Brainstorming）**
```
1. 探索项目上下文（文件、文档、最近提交）
2. 一次问一个问题，优先多选题
3. 提出 2-3 个方案，说明权衡 + 推荐
4. 分段展示设计文档，逐段确认
5. 保存设计文档 → docs/plans/YYYY-MM-DD-<topic>-design.md
```

**阶段 2：编写计划（Writing Plans）**
```
1. 拆分为 2-5 分钟的小任务
2. 每个任务包含：
   - 明确的文件路径
   - 完整的代码示例
   - 验证步骤
3. 保存到 docs/plans/YYYY-MM-DD-<feature>.md
4. 提供两种执行模式：
   - 子代理驱动（推荐）
   - 手动执行
```

**阶段 3：子代理执行（Subagent-Driven）**
```
每个任务循环：
1. 派发实现子代理
2. 等待完成
3. 派发规范审查子代理 → 确认符合规范
4. 派发代码质量审查子代理 → 确认质量达标
5. 修复问题（如有）
6. 标记完成，进入下一个任务
```

**硬约束**：
- ❌ 禁止在用户批准设计前写代码
- ❌ 禁止跳过 TDD 流程
- ❌ 禁止一次性提交所有代码（必须小步提交）

**来源**：Superpowers `brainstorming` + `writing-plans` + `subagent-driven-development` skills

---

*此模板由 task-monitor 插件自动注入*
*版本：1.0.0*
*注入时间：{timestamp}*