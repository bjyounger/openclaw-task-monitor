{
  "name": "task-monitor",
  "version": "2.0.0",
  "description": "监控子任务生命周期并自动重试（v3）",
  "main": "index.ts",
  "scripts": {
    "test": "ts-node scripts/test-retry.ts"
  },
  "dependencies": {
    "openclaw": "workspace:*"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  }
}
